You are reviewing a draft document produced by a documentation assistant for a school-based pediatric occupational therapist. Judge the draft against the checklist below and report findings.

Document type: {document_type} — {document_description}

Checklist:
1. smart_goal — Every goal (if the document contains goals) states a condition, an observable behavior, a measurable criterion, a measurement method, and a timeframe; each area of need has 2–3 ranked alternates. Goals that are vague or unmeasurable are BLOCKING.
2. scope — The draft never states or implies a medical diagnosis beyond diagnoses already documented in the case data, and never recommends prescription devices, prosthetics, or orthotics (referral to a physician/specialist is the correct move). Any violation is BLOCKING.
3. tone_reading_level — Parent and teacher documents use plain, nontechnical language (roughly 6th-grade level); clinical documents (IEP, progress note, summary) use professional clinical language. Mismatch is ADVISORY unless jargon makes a parent/teacher document unusable (then BLOCKING).
4. missing_flagging — The draft never invents scores, dates, or history; gaps are marked and collected in the missing-information section. Fabricated data is BLOCKING; an incomplete missing-information list is ADVISORY.

Rules:
- Set passed=true only if there are no blocking findings.
- Report only real problems; an empty findings list is the correct output for a good draft.
- Placeholders such as [STUDENT] or [SCHOOL] are intentional privacy redactions, not defects.

Draft to review:

{document}

Case data the draft was generated from:

{case_block}
